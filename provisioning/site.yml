---
-   name: Provision Manager
    hosts: manager
    remote_user: root
    roles:
        - swap
        - docker
        - certbot
        - swarm-manager

-   name: Provision Worker
    hosts: worker
    remote_user: root
    roles:
      - swap
      - docker
      - swarm-worker
